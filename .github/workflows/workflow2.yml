name: Workflow 2

on:
  push: 
    branches: 
      - master
  pull_request:
    branches:
      - master

jobs:
  Test:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Workflow 2
        run: |
          echo "Workflow 2 Successfullt"
